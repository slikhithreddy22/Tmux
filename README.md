# Tmux
This config suits for you if you love gruvbox theme, incase if you dont like just change the colors in the statusline.conf file.
Just clone this repo to `~/.config/tmux` and move that tmux.conf file by `cp tmux.conf ~/.tmux.conf` and then delete this tmux.conf in this folder
